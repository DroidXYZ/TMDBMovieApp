package com.example.tmdbmovies.base

import dagger.android.support.DaggerAppCompatActivity

// BaseActivity to create separate layer before extending DaggerAppCompatActivity
open class BaseActivity : DaggerAppCompatActivity() {
}